'use strict';

/**
 * configurationglobale router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::configurationglobale.configurationglobale');
