'use strict';

/**
 * configurationportfolio router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::configurationportfolio.configurationportfolio');
